// Definindo propriedades da partícula
speed = random_range(1, 3); // Velocidade aleatória
direction = random(360);    // Direção aleatória em graus
lifetime = 15 + irandom(15); // Duração da partícula
age = 0;                     // Contador de tempo
