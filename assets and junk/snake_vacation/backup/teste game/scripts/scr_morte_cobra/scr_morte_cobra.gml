function morte_cobra(){
	// Verifica colisão com o próprio corpo (exclui o primeiro segmento se ele for a cabeça)
	/*if place_meeting(x, y, obj_corpo) {
		var inst = instance_place(x, y, obj_corpo);
		if inst != noone and inst.index < global.segmento - 5 {
			instance_destroy();
		}
	}

	// Verifica colisão com blocos
	if place_meeting(x, y, obj_block) {
		instance_destroy();
	}*/
}