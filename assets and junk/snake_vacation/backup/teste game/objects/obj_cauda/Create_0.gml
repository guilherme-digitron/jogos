pos = [0, 0];  // Inicializa pos_body como um array com valores padrões [x=0, y=0]
// Serve para garantir que pos_body sempre tenha um valor válido, mesmo antes de ser atualizado com posições reais
pos_ant = 0
index = 0; // Inicializa index como 0, para evitar uso de variável indefinida